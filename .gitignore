# Terraform
# Note: .terraform.lock.hcl not explicitly ignored, as it is recommended to commit this file to version control
.terraform/
*.tfstate
*.tfstate.*
crash.log
terraform.tfvars
terraform.tfvars.json

# Local override files
*.auto.tfvars
*.override.tf
*.override.tf.json

# Terraform crash logs (verbose)
crash.log
crash.*.log

# Terraform plan artifacts
*.tfplan
tfplan

# PowerShell
*.ps1xml
*.pssc
*.psm1~    # temp backup files
*.ps1~     # temp backup files
*.ps1.bak
*.ps1.txt

# DSC
*.mof
*.checksum
*.meta.mof
DSCMetaConfigs/
# Optional: ignore compiled DSC directory if used
DSC/

# OS/system files
Thumbs.db
.DS_Store
ehthumbs.db
desktop.ini

# Shell scripts and artifacts
*.log
*.bak
*.swp
*.tmp

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace


# Windows/Linux artifacts
*.lnk
# *.lock ## Removed for now as it's a bit aggressive.
*.pid

# Git-specific
.git/
!.gitignore

# Secrets (just in case)
secrets.json
*.key
*.pem
*.pfx
*.cert
*.env
.env*

# Docs/Markdown tooling
# *.html
_site/
.cache/

# Packer (if used in the future)
packer_cache/
*.pkrvars.hcl

# Python virtual envs (if you do use python tooling)
__pycache__/
*.py[cod]
.venv/

# Rando null file on my pc for some damn reason
null

# Claude/AI Working Files
.claude/worktrees/
.claude/settings.local.json